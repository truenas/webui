/**
 * The `cssClass` the tier service uses to locate the tier column for un-hiding when tiering is
 * enabled. Exported so both the column declaration and `SharingTierService.enableTierColumn`
 * share the same literal.
 */
export const tierColumnCssClass = 'tier-cell';
