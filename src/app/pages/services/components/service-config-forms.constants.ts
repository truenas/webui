import { marker as T } from '@biesbjerg/ngx-translate-extract-marker';

/**
 * Snackbar copy every service config form shows on a successful save. Shared so the extraction
 * marker lives in one place rather than being repeated verbatim in each `handleSubmit`.
 */
export const serviceConfigSavedMessage = T('Service configuration saved');
