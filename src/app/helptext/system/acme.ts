import { marker as T } from '@biesbjerg/ngx-translate-extract-marker';

export const helptextSystemAcme = {
  editTitle: T('Edit DNS Authenticator'),
  addTitle: T('Add DNS Authenticator'),

  name: T('Name'),
  nameTooltip: T('Internal identifier for the\
 authenticator.'),

  provider: T('Authenticator'),
  providerTooltip: T('Choose a DNS provider and\
 configure any required authenticator attributes.'),
};
