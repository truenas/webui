import { T } from '../../../translate-marker';

export default {
    filesystem_placeholder : T('Base Filesystem'),
    filesystem_tooltip : T('Select the base filesystem.'),

    launchbutton_name : T('Launch DS Operator'),
    snackbar_message : T('Asigra successfully updated.')
}