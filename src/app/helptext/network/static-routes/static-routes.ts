import { marker as T } from '@biesbjerg/ngx-translate-extract-marker';

export const helptextStaticRoutes = {
  destinationTooltip: T('Use the format <i>A.B.C.D/E</i> where <i>E</i> is the CIDR mask.'),
  gatewayTooltip: T('Enter the IP address of the gateway.'),
};
