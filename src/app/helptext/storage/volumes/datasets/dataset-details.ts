import { marker as T } from '@biesbjerg/ngx-translate-extract-marker';

export const datasetDetailsHelptext = {
  rootDatasetTooltip: T('The root dataset is the top-level container in your pool, sharing the same name\
 as the pool itself. <br><br>When managing your TrueNAS system, it\'s generally best practice\
 to create dedicated datasets under the root dataset for different types of data, rather than\
 storing data directly in the root dataset itself.'),
};
