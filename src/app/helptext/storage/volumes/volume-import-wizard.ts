import { marker as T } from '@biesbjerg/ngx-translate-extract-marker';

export const helptextImport = {
  title: T('Import Pool'),

  selectPool: T('Select pool to import'),
  poolLabel: T('Pool'),
};
