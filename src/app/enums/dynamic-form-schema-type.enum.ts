export enum DynamicFormSchemaType {
  Input = 'input',
  Select = 'select',
  Explorer = 'explorer',
  Checkbox = 'checkbox',
  List = 'list',
  Dict = 'dict',
  Ipaddr = 'ipaddr',
  Cron = 'cron',
  Uri = 'uri',
  Enum = 'enum',
  Text = 'text',
}
