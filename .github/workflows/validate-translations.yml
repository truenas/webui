name: WebUI
on:
  push:
    branches:
      - master
    paths:
      - 'src/assets/i18n/**'
  pull_request:
    branches:
      - '**'
    paths:
      - 'src/assets/i18n/**'
      # TEST-ONLY: normally this workflow never fires on a .github-only PR, so its
      # two `prepare` call sites would go unexercised. Remove with the rest of
      # this branch — it must not reach master.
      - '.github/workflows/**'

permissions:
  contents: read

# Literal group, not `${{ github.workflow }}` — this workflow and main.yml are
# both named "WebUI", so keying on the name would let them cancel each other on
# a PR that touches both translations and source.
concurrency:
  group: webui-validate-translations-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  install:
    name: Checkout and Install
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install
        uses: iXsystems/ux-github-workflows/.github/actions/prepare@feat/shared-claude-review
        with:
          cache-jest: 'true'

  lint-translations:
    name: Validate translation strings
    needs: [install]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install
        uses: iXsystems/ux-github-workflows/.github/actions/prepare@feat/shared-claude-review
        with:
          cache-jest: 'true'
      - name: Validate
        run: yarn run validate-translations
